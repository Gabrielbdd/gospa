// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: gospa/install/v1/install.proto

package installv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/Gabrielbdd/gospa/gen/gospa/install/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// InstallServiceName is the fully-qualified name of the InstallService service.
	InstallServiceName = "gospa.install.v1.InstallService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// InstallServiceGetStatusProcedure is the fully-qualified name of the InstallService's GetStatus
	// RPC.
	InstallServiceGetStatusProcedure = "/gospa.install.v1.InstallService/GetStatus"
	// InstallServiceInstallProcedure is the fully-qualified name of the InstallService's Install RPC.
	InstallServiceInstallProcedure = "/gospa.install.v1.InstallService/Install"
)

// InstallServiceClient is a client for the gospa.install.v1.InstallService service.
type InstallServiceClient interface {
	// GetStatus returns the current install state of the singleton workspace.
	GetStatus(context.Context, *connect.Request[v1.GetStatusRequest]) (*connect.Response[v1.GetStatusResponse], error)
	// Install transitions the workspace out of not_initialized and starts
	// the provisioning pipeline. Idempotent-ish: returns FailedPrecondition
	// when already ready or currently provisioning.
	Install(context.Context, *connect.Request[v1.InstallRequest]) (*connect.Response[v1.InstallResponse], error)
}

// NewInstallServiceClient constructs a client for the gospa.install.v1.InstallService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewInstallServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) InstallServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	installServiceMethods := v1.File_gospa_install_v1_install_proto.Services().ByName("InstallService").Methods()
	return &installServiceClient{
		getStatus: connect.NewClient[v1.GetStatusRequest, v1.GetStatusResponse](
			httpClient,
			baseURL+InstallServiceGetStatusProcedure,
			connect.WithSchema(installServiceMethods.ByName("GetStatus")),
			connect.WithClientOptions(opts...),
		),
		install: connect.NewClient[v1.InstallRequest, v1.InstallResponse](
			httpClient,
			baseURL+InstallServiceInstallProcedure,
			connect.WithSchema(installServiceMethods.ByName("Install")),
			connect.WithClientOptions(opts...),
		),
	}
}

// installServiceClient implements InstallServiceClient.
type installServiceClient struct {
	getStatus *connect.Client[v1.GetStatusRequest, v1.GetStatusResponse]
	install   *connect.Client[v1.InstallRequest, v1.InstallResponse]
}

// GetStatus calls gospa.install.v1.InstallService.GetStatus.
func (c *installServiceClient) GetStatus(ctx context.Context, req *connect.Request[v1.GetStatusRequest]) (*connect.Response[v1.GetStatusResponse], error) {
	return c.getStatus.CallUnary(ctx, req)
}

// Install calls gospa.install.v1.InstallService.Install.
func (c *installServiceClient) Install(ctx context.Context, req *connect.Request[v1.InstallRequest]) (*connect.Response[v1.InstallResponse], error) {
	return c.install.CallUnary(ctx, req)
}

// InstallServiceHandler is an implementation of the gospa.install.v1.InstallService service.
type InstallServiceHandler interface {
	// GetStatus returns the current install state of the singleton workspace.
	GetStatus(context.Context, *connect.Request[v1.GetStatusRequest]) (*connect.Response[v1.GetStatusResponse], error)
	// Install transitions the workspace out of not_initialized and starts
	// the provisioning pipeline. Idempotent-ish: returns FailedPrecondition
	// when already ready or currently provisioning.
	Install(context.Context, *connect.Request[v1.InstallRequest]) (*connect.Response[v1.InstallResponse], error)
}

// NewInstallServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewInstallServiceHandler(svc InstallServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	installServiceMethods := v1.File_gospa_install_v1_install_proto.Services().ByName("InstallService").Methods()
	installServiceGetStatusHandler := connect.NewUnaryHandler(
		InstallServiceGetStatusProcedure,
		svc.GetStatus,
		connect.WithSchema(installServiceMethods.ByName("GetStatus")),
		connect.WithHandlerOptions(opts...),
	)
	installServiceInstallHandler := connect.NewUnaryHandler(
		InstallServiceInstallProcedure,
		svc.Install,
		connect.WithSchema(installServiceMethods.ByName("Install")),
		connect.WithHandlerOptions(opts...),
	)
	return "/gospa.install.v1.InstallService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case InstallServiceGetStatusProcedure:
			installServiceGetStatusHandler.ServeHTTP(w, r)
		case InstallServiceInstallProcedure:
			installServiceInstallHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedInstallServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedInstallServiceHandler struct{}

func (UnimplementedInstallServiceHandler) GetStatus(context.Context, *connect.Request[v1.GetStatusRequest]) (*connect.Response[v1.GetStatusResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("gospa.install.v1.InstallService.GetStatus is not implemented"))
}

func (UnimplementedInstallServiceHandler) Install(context.Context, *connect.Request[v1.InstallRequest]) (*connect.Response[v1.InstallResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("gospa.install.v1.InstallService.Install is not implemented"))
}
